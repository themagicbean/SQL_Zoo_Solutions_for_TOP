SELECT name, continent, population FROM world

SELECT name FROM world WHERE population >=  200000000

SELECT name, gdp/population FROM world WHERE population >= 200000000
//Note some solutions use "AS: but it is not required to pass SQL Zoo

SELECT name, population/1000000 FROM world WHERE continent = 'South America';
//same

SELECT name, population FROM world WHERE name IN ('France', 'Germany', 'Italy');

SELECT name FROM world WHERE name LIKE '%United%';
//Note MSAccess uses *s

SELECT name,population, area FROM world WHERE population > 250000000 OR area > 3000000;

SELECT name, population, area
FROM world
WHERE (population > 250000000 OR area > 3000000)
AND NOT (population > 250000000 AND area > 3000000);

//Conflicting info -- W3 schools and sample solutions use XOR
//But that is not working on SQL ZOO
// Using (or) and not (and) works

SELECT name, ROUND(population/1000000, 2), ROUND(gdp/1000000000, 2) FROM world WHERE continent = 'South America'

SELECT name, ROUND(gdp/population, -3) AS "per-capita GDP" FROM world WHERE gdp >= 1000000000;

//ex11
